    

    x = point.x;
    y = point.y;
    
    // 圆心坐标
     circle.x = 200;
     circle.y = 200;

    function getAngle(point,circle) (

     var angle=arctan(y2-y1)/(x2-x1)

    	return angle

    )
